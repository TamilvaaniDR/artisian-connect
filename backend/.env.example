# FastAPI + MySQL configuration
APP_NAME=artisan-connect-api
APP_ENV=development

# CORS (comma separated)
CORS_ORIGINS=http://localhost:3000

# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=artisan_connect

# Auth (placeholder)
JWT_SECRET=change_me
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=60
